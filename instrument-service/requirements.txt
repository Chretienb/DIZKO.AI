# CPU-only torch keeps the image small (no CUDA) — Railway runs CPU.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2
panns-inference==0.1.1
librosa==0.10.2
soundfile==0.12.1
# 0.110.x avoids fastapi-cli -> httpx -> httpcore, whose graph the pip resolver
# can't satisfy (build failed with ResolutionImpossible). Lighter + resolves fast.
fastapi==0.110.3
starlette==0.37.2
uvicorn[standard]==0.30.1
requests==2.32.3
# Pin numpy<2 — panns/torchlibrosa aren't built against numpy 2.x (ABI clash).
numpy<2
