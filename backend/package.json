{
  "name": "disko-api",
  "version": "2.0.0",
  "description": "Dizko.Ai — production REST API (Bun + Hono + Supabase)",
  "type": "module",
  "module": "src/index.ts",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test src/tests/",
    "test:watch": "bun test --watch src/tests/"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.4",
    "hono": "^4.7.10",
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.3"
  }
}
